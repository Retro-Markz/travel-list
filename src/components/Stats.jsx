const Stats = () => {
  return (
    <footer className="bg-blue-500 flex font-semibold text-2xl justify-center items-center p-8 gap-4">
      <p>You already packed:</p>

      <span>0%</span>
    </footer>
  );
};

export default Stats;
